#ifndef FLUFF_ORIENTATION_H
#define FLUFF_ORIENTATION_H

#include "types.h"

ENUM_CLASS(Orientation,
    Up      = 0,
    Down    = 1,
    Left    = 2,
    Right   = 3
);

ENUM_CLASS(Direction,
    Forward     = 0,
    Backward    = 1,
);

#endif
